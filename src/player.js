class Player {
  constructor(player, token) {
    this.id = player
    this.token = token;
    this.wins = []];
  }

  saveWinsToStorage() {
    localStorage.setItem();
    JSON.stringify();
  }

  retrieveWinsFromStorage() {
    JSON.parse();
    localStorage.getItem();
  }
}
